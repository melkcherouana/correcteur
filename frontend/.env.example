# URL de l'API. En dev, laisser vide : le proxy Vite gère le /api
# (voir vite.config.js). En prod, renseigner l'URL complète du backend
# déployé, avec le suffixe /api (ex: https://evalpro-api.onrender.com/api).
VITE_API_URL=
